import SwiftUI

// ContentView is unused — the app launches directly into ChatView via OpenClawChatApp.
// Keeping this file as xcodegen expects at least one view file in App/.
struct ContentView: View {
    var body: some View {
        EmptyView()
    }
}
